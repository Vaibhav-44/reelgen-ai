# ReelaiGen

ReelaiGen is an agentic pipeline for turning a PDF into a short educational animated video in the style of 3Blue1Brown.

The repository has been trimmed down to the reusable Manim helper layer from the earlier prototype. The remaining source lives in `src/manim_helpers/` and can be used as a clean starting point for the next implementation phase.
